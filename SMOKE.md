# Load-unpacked smoke

Human checklist after `chrome://extensions` → Developer mode → **Load unpacked** on this folder (the one with `manifest.json`).

## Keep-installed (no demo tabs)

1. Pin Semblance. Open the popup. Do **not** open Beat A or Beat B yet.
2. Scope coach lists Google and Microsoft scopes in plain sentences. Search `Mail.Send`.
3. Paste `http://127.0.0.1:4173/callback?code=4/0Aean5NotARealCodeAtAll0001` → **Check this paste**. Expect localhost and code hits. Nothing stored.
4. Paste `hello` → no ritual match. The box is unchanged.
5. Friend-verify: save a word, type the wrong word → gate stays closed. Type the right word → gate opens.
6. **I understand** still works as the other key, without opening theater.
7. Revoke buttons open Google connections and Microsoft app-access (sign-in on those pages is theirs).

## Theater, then back to the product

8. From the same popup, **Open Beat A · friend lure**. Chat cannot send. LabQueue is labeled a prop.
9. Open LabQueue / **Open Beat B · fake Allow**. Sticky banner **FAKE — not Google/Microsoft** stays on top, including over the pause sheet.
10. If the gate is already open from step 5, Allow goes to aftermath — you already owned the click. Still no network. LabQueue is still a prop.
11. To see pause → hard-stop → quiet: clear the shared word (empty Save word) so the gate closes, reload Beat B. The pause sheet may already be up. Esc or click the dim area — do not tap I understand. Allow → hard-stop. Esc. Allow again → quiet, no nag sheet. Gate still closed.
12. Close every demo tab. Open the popup again. Scope, paste, friend-verify, and revoke still work. Theater was optional.
