# Semblance

Chrome extension that pauses at OAuth **Allow** — plain-language scopes, friend verify, and one-tap revoke shortcuts.

Load unpacked from this repo after install instructions land in a follow-up commit.
